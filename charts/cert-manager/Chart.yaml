apiVersion: v2
name: cert-manager-wrapper
description: GitOps wrapper for the upstream cert-manager Helm chart
type: application
version: 0.1.0

# renovate: datasource=helm depName=cert-manager registryUrl=https://charts.jetstack.io
dependencies:
  - name: cert-manager
    repository: https://charts.jetstack.io
    version: v1.21.1
