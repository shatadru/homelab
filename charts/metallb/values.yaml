metallb:
  # This homelab uses Layer 2 advertisement only; BGP is not required.
  frrk8s:
    enabled: false
  speaker:
    frr:
      enabled: false

  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: "200m"
      memory: "256Mi"
