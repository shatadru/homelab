cloudnative-pg:
  webhook:
    startupProbe:
      failureThreshold: 60
      periodSeconds: 50

  resources:
    requests:
      cpu: 100m
      memory: 128Mi
    limits:
      cpu: "200m"
      memory: "256Mi"
