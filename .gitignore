# Kubernetes credentials / secrets
*.kubeconfig
kubeconfig
k3s.yaml
*.key
*.crt
*.pem

# Local environment
.env
.env.*

# Ansible
*.retry
.vault-password

# Temporary files
tmp/
.tmp/
*.tmp

# Database dumps and generated inventory reports (see immich-inven.sh)
*.sql
*.sql.gz
immich_backup_*
immich_inventory_*.md

# Python tooling
.tox/
__pycache__/
*.pyc

# Helm vendored dependencies (rebuilt by `tox -e helm` / `helm dependency build`)
charts/*/Chart.lock
charts/*/charts/
!charts/nfs-csi/Chart.lock
!charts/nfs-csi/charts/

# Editor / OS
.vim/
*.swp
.DS_Store
